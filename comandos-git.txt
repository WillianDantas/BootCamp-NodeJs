git add -A && git commit -m "Your Message"
git status
git ini
.gitignore

-------

git init
git clone
git add.
git add index.html
git add
git push
git pull
git pull origin master
git merge <branch> => juntar branch
git ststus
git log
git branch


git remote add origin  http://
git remote rm origin
git push -u origin master
git push origin <Branch> => Criar branch no github / comando envia uma branch do repositório local ao repositório remoto
git remote add origin  https://github.com/WillianDantas/Tecnicas-Avancadas_Angular.git

git pull origin master --allow-unrelated-histories

git
git checkout master
git branch -D contato = deletar branch
git branch contato
git checkout -b  contato

touch  contato.html
git add contato.html
git commit -m 'Criando arquivo html de contato'
git commit -a -m <comentario> => adicionar arquivo e fazer commit em um passo so.
git log 
git log -stat => verificar historico e sua estatiticas

git rabase <nome_arquivo> => comando para juntar aquivo de duas branchs sem gerar um novo evento.

git remote -v => todos os repositórios remotos sejam listados, até a URL do repositório.
git remote => comando utilizado para ver um repositório remoto.

git tag => comando é utilizado para marcar compromissos específicos no Git

git diff <nome_arquivo> => comando se utiliza para ver as modificações feitas no arquivo.

git config --global user.name "SEU NOME" - git config --global user.email "SEU ENDEREÇO DE E-MAIL" => configurar usuario e e-mail na sua máquina.

git checkout -- NOME_DO_ARQUIVO = > comando se deve utilizar quando você quer 'resetar' as alterações de um arquivo.

git reset HEAD NOME_DO_ARQUIVO => comando você utiliza para retirar um arquivo adicionado para commitar (após utilizar git add).

git commit --amend => comando utilizado para editar a mensagem do último commit.

README => Feito Markdonw.
 
clear => limpar terminal.
https://pandao.github.io/editor.md/en.html => editor README.

https://imgur.com/ => fazer upload de imagens.

https://simpleicons.org/?q=google

https://www.youtube.com/watch?v=T70t3mDiwvg
